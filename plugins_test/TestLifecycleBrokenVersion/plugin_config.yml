description: Phase 4 static fixture — plugin_config.yml WITHOUT a version field (B-007 trigger)
remote: True
arguments:
endpoints:
  ping:
    internal_name: ping
    tags:
    remote: True
    accessible_by_other_plugins: True
    description: Trivial endpoint; the fixture's point is its MISSING version field, not this endpoint
    arguments:

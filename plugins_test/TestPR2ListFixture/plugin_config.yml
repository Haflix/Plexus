description: PR2 test fixture — intentional list-form endpoints (legacy shape; should be rejected at load)
version: 0.1.0
remote: false
arguments:
endpoints:
  - access_name: ping
    internal_name: ping
    remote: false
    accessible_by_other_plugins: true
    description: Returns pong

description: Phase 4 static fixture — was the boot-time witness for B-007's load-loop abort; unused since the case moved to an on-demand load, kept for a future B-093 guard
version: 0.1.1
remote: True
arguments:
endpoints:
  get_loaded:
    internal_name: get_loaded
    tags:
    remote: True
    accessible_by_other_plugins: True
    description: Returns self.loaded — True if on_load ran
    arguments:

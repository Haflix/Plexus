description: Phase 5 fixture — remote=False plugin used to verify the framework's plugin.remote=False gate blocks remote callers
version: 0.3.0
remote: False
arguments:
endpoints:
  r_local_only:
    internal_name: r_local_only
    tags:
    remote: False
    accessible_by_other_plugins: True
    description: Plugin-level remote=False blocks remote callers (contract test)
    arguments:
      - name: value

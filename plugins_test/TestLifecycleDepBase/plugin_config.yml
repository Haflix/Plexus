description: HUNT-076 fixture — trivial dependency base for the reload dep-precheck regression
version: 1.0.0
remote: False
arguments:
endpoints:
  ping:
    internal_name: ping
    tags:
    remote: False
    accessible_by_other_plugins: True
    description: Returns True — liveness probe
    arguments:
